export class MiddlewareHandler {
  constructor(props) {
    this.handler = props.handler;
    this.nestingLevel = props.nestingLevel;
    this.regexp = new RegExp(props.nestingLevel ? `^${props.path}.*?$` : '.*');
  }
}
//# sourceMappingURL=middleware-handler.mjs.map